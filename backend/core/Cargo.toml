[package]
name = "calculi-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ts-rs = { version = "12.0.0", features = ["format"] }
owo-colors = "4.3.0"
